# Batman OS — variáveis de ambiente do LLM Gateway (Vol.II Cap.8, Vol.IX
# Reference Implementation). Copie para `.env` e preencha os valores reais
# — `.env` nunca é commitado (ver .gitignore).
#
# Nenhuma destas variáveis é necessária para rodar `pytest`/`batman scan`:
# o LLM Gateway só é consultado quando o Kernel roda uma Mission real e nem
# regra nem humano resolvem um DecisionPoint a tempo (hierarquia Knowledge
# First -> Human Last -> LLM Last). Sem ANTHROPIC_API_KEY, uma tentativa de
# uso real levanta erro de autenticação da própria Anthropic — comportamento
# esperado, não um bug do batman-os.

# Chave da API Anthropic — nunca inline em código/config, só aqui.
ANTHROPIC_API_KEY=

# Modelo usado pelo LLM Gateway.
LLM_MODEL=claude-haiku-4-5-20251001

# Limite de tokens de saída por chamada.
LLM_MAX_TOKENS=1024

# Timeout (segundos) por chamada à API Anthropic.
LLM_TIMEOUT=30.0

# Teto diário de custo de LLM (USD) — circuit breaker fail-closed
# (batman_os.llm.cost_tracker): se o ledger de custo existir mas não puder
# ser lido, o Gateway BLOQUEIA novas chamadas em vez de liberar gasto sem
# conseguir contabilizar.
MAX_DAILY_LLM_COST_USD=10.0

# Provedor do LLM Gateway (batman_os.llm.fallback_gateway):
#   anthropic    — só Anthropic (default, comportamento original)
#   local-first  — modelo local (GGUF) primeiro; qualquer falha (timeout,
#                  JSON inválido, confiança baixa) cai para a Anthropic
#   local-shadow — Anthropic decide; o local roda em sombra e grava
#                  concordância para o relatório de promoção (ADR-0011)
#   local-only   — SEM Anthropic: o modelo local é o único LLM da cadeia;
#                  se ele falhar, o DecisionPoint escala para humano.
#                  Não requer ANTHROPIC_API_KEY.
# Os modos local-* exigem o extra: pip install "batman-os[local-llm]"
LLM_PROVIDER=anthropic

# Caminho do GGUF do modelo local (Qwen2.5-3B fine-tunado, quantização Q6_K
# — Q4_K_M degradou qualidade no experimento do radar-preditivo).
LLM_LOCAL_GGUF_PATH=

# Timeout (segundos) por geração local em CPU. Um timeout DESARMA o gateway
# local pelo resto do processo (llama.cpp não cancela geração em andamento).
LLM_LOCAL_TIMEOUT=60.0

# Confiança mínima da resposta local; abaixo disso a chamada escala para a
# Anthropic ("saber quando não sabe" é comportamento treinado do modelo).
LLM_LOCAL_MIN_CONFIDENCE=0.75

# Threads de CPU para a inferência llama.cpp.
LLM_LOCAL_N_THREADS=4

# Chaves de API da API HTTP do batman-os (Fase 8 do roadmap de plataforma,
# `api/auth.py`) — objeto JSON tenant->chave. Toda requisição a /missions/*
# e /jobs/* exige header `Authorization: Bearer <chave>`; sem esta variável
# (ou vazia), NENHUMA chave é válida — nunca há um default inseguro
# implícito. Gere uma chave nova com:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
# Texto puro aqui é deliberado (mesmo tratamento de ANTHROPIC_API_KEY acima)
# — o batman-os ainda não tem deploy real; revisar (hashing em repouso)
# quando houver.
BATMAN_API_KEYS={"local": ""}
