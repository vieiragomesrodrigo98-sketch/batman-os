"""CLI do Batman OS — resolve a referência `batman = "batman_os.cli.batman:main"`
declarada em `pyproject.toml` (antes deste pacote existir, apontava para um
módulo inexistente — confirmado por execução real, `ModuleNotFoundError`).
"""
