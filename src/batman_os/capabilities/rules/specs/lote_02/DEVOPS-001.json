{
  "regra": {
    "codigo": "DEVOPS-001",
    "agente": "devops-engineer",
    "severidade": "high",
    "categoria": "segredos",
    "titulo": "Segredo/token hardcoded no código",
    "causa": "Credenciais ou tokens literais no código-fonte ficam expostos no histórico do git e em qualquer clone do repositório — qualquer pessoa com acesso ao repo pode extraí-los.",
    "remediacao": "Mover para variável de ambiente e ler via config/settings.py. DoD: grep por padrões de secret retorna vazio fora de .env.example.",
    "modo": "presenca",
    "pattern": "(api[_-]?key|secret[_-]?key|password|passwd|token|auth[_-]?token)\\s*=\\s*['\"][^'\"]{8,}['\"]",
    "ignore_case": true
  },
  "descoberta": {
    "tipo": "arvore",
    "scope_dirs": ["api", "src", "dashboard", "pages"],
    "extensoes": [".py"],
    "excluir_caminho_contem": ["test", "example", ".env"]
  }
}
